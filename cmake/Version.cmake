# set configure file with version code
set (VERSION_MAJOR 0)
set (VERSION_MINOR 1)
set (VERSION_PATCH 0)

set(cppsocket_version "${VERSION_MAJOR}.${VERSION_MINOR}.${VERSION_PATCH}")
message("cppsocket_version : " ${cppsocket_version})
